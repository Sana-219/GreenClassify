# GreenClassify - Production Requirements (for Vercel)
# Optimized for serverless deployment

# Web Framework
flask>=2.0.0
werkzeug>=2.0.0

# Core Dependencies
numpy>=1.21.0

# Deep Learning (must be compatible with Vercel's size limits)
tensorflow>=2.10.0,<2.14
keras>=2.10.0

# Image Processing
Pillow>=8.0.0

# Utilities
python-dotenv>=0.19.0
